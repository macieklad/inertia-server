# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

## Adding a changeset

Run `bun changeset` to create a new changeset describing your changes.

## Releasing

When changesets are merged to master, a "Version Packages" PR will be created automatically. Merging that PR will publish new versions to npm.
